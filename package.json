{
  "name": "cf-ready",
  "private": true,
  "scripts": {
    "build": "react-router build",
    "dev": "shopify app dev",
    "preview": "npm run build && vite preview",
    "typegen": "mkdir -p .wrangler/types && wrangler types .wrangler/types/worker-configuration.d.ts && react-router typegen",
    "typecheck": "npm run typegen && tsc --noEmit",
    "lint": "oxlint .",
    "format": "oxfmt --write app scripts workers tests react-router.config.ts vite.config.ts vitest.config.ts",
    "format:check": "oxfmt --check app scripts workers tests react-router.config.ts vite.config.ts vitest.config.ts",
    "docs:check": "node scripts/check-docs.mjs",
    "test": "vitest run",
    "doctor": "react-doctor",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "check": "npm run docs:check && npm run lint && npm run format:check && npm run typecheck && npm test && npm run doctor && npm run build && wrangler deploy --dry-run",
    "config:link": "shopify app config link",
    "generate": "shopify app generate",
    "config:use": "shopify app config use",
    "env": "shopify app env",
    "shopify": "shopify",
    "vite": "vite"
  },
  "type": "module",
  "engines": {
    "node": ">=26.5.0 <27"
  },
  "dependencies": {
    "@shopify/shopify-api": "13.1.0",
    "@shopify/shopify-app-react-router": "1.2.1",
    "@shopify/shopify-app-session-storage": "5.0.1",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-router": "7.18.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.47.0",
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "@react-router/dev": "7.18.1",
    "@shopify/polaris-types": "1.0.7",
    "@types/node": "26.1.2",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "oxfmt": "0.61.0",
    "oxlint": "1.76.0",
    "react-doctor": "0.9.2",
    "typescript": "6.0.3",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  },
  "workspaces": [
    "extensions/*"
  ],
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "Matteo"
}
